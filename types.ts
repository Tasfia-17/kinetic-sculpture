
export interface SculptureSettings {
  elementCount: number;
  sculptureRadius: number;
  lineLength: number;
  lineWidth: number;
  baseSpeed: number;
  speedIncrement: number;
  startColor: string;
  endColor: string;
}
